server:
  port: 5555

spring:
  application:
    name: gateway
  cloud:
    config:
      uri: http://localhost:8888

eureka:
  client: 
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/   