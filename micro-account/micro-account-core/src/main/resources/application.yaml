server:
  port: 8012

spring:
  application:
    name: account
  cloud:
    config:
      uri: http://localhost:8888

eureka:
  client: 
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/   